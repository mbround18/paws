[project]
name = "python-no-tests-fixture"
version = "0.1.0"
description = "A uv project with no test suite and no pytest, like a real app repo that only ships a build"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
