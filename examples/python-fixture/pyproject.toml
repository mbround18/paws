[project]
name = "python-fixture"
version = "0.1.0"
description = "Minimal Python fixture project for paws-audit/paws-provision to detect and run"
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
