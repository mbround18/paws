[project]
name = "python-fixture"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "MBRound18", email = "michael.bruno1337@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
python-fixture = "python_fixture:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
