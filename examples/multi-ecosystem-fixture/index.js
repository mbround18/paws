// Node half of the multi-ecosystem fixture: co-located with a Cargo.toml/src/
// Rust crate in this same directory to simulate a repo with both a Rust crate
// and a Node project, for paws-provision's concurrent multi-ecosystem provisioning.
export function subtract(a, b) {
  return a - b;
}
